# AJOS Daily Highlights — 2026-06-11

## Top 10

1. **Plan 3 shipped — cloud-first discovery** — `config.json`, Discovery UI panel, `CLOUD_DISCOVERY_AUTOMATION.md`.
2. **Search scope from dashboard** — countries, themes, industries, pause, caps; push config to GitHub for cloud agent.
3. **First real discovery run** — OpenUp, Auntie, Kyan Health still **pending** review in queue.
4. **Plan 2 foundation** — `discovery_engine.py`, Discovered badge, Pass/Save/Interested merge flow.
5. **Cursor Automation ready to enable** — cron `0 8,20 * * *`, repo `opportunity-ajos`, paste prompt from docs.
6. **Optional Slack notify** — documented for score ≥ threshold in cloud automation.
7. **Git + Streamlit deploy unblocked** — SSH push to `main` works.
8. **Human-in-the-loop** — agent commits candidates only; you review on phone/dashboard.
9. **Config caveat** — Streamlit saves local; cloud agent reads **committed** `config.json` on GitHub.
10. **Legacy `/loop`** — dev only; production = Cursor Cloud Automation.

---

## Session log

**Decided**
- Background agent runs while **Cursor IDE is open** (`/loop 3h`); not 24/7 daemon yet.
- Strong matches notify via **dashboard queue + desktop** (`notify-send`, score ≥ 85, max 1/run).
- CSV updates only on **Save or Interested** — Pass rejects discovery candidates permanently.
- UX Plan 1: one-card review flow, English chrome, Hinglish bullets (Graphy + Futurice seeds).
- Next build: **Discovery Control Panel** — add/remove countries, themes, industry tags from UI.
- Highlights: **one file per day**, top 10 up front, 10–20 detail bullets below.

**Built & shipped**
- `discovery_engine.py` — dedupe, `add_candidate`, `approve_candidate`, `reject_candidate`, `build_review_queue`, CLI.
- `data/discovery/candidates.json` + `runs.json` — 3 pending wellness companies, 1 logged run.
- `scripts/notify_discovery.py` — Linux desktop alerts.
- `app.py` — discovered items first in queue, Discovered badge, discovery footer with last run.
- Hinglish action copy + password gate + README discovery section (`22d104a`).
- Review UI polish — primary CTA, escaped card HTML (`70b5442`).
- Git remote switched to SSH; discovery stack pushed to GitHub for Streamlit rebuild.

**Learned & open**
- `/loop` was **not left running** — only one manual research run so far.
- Streamlit cloud ≠ local — feedback and actions don't sync unless committed or shared storage added.
- "Industries" don't exist yet — only **themes**; industry tags are a Plan 3 addition.
- OpenUp / Auntie / Kyan Health waiting in queue — review when you open AJOS.
- `proposals.json` timestamp-only changes excluded from commits (runtime noise).
- Optional later: Cursor SDK daemon, Telegram alerts, founder-level discovery (ROADMAP V4).

**Plan 3 built (cloud discovery)**
- `data/discovery/config.json` + `load_config` / `save_config` in `discovery_engine.py`.
- AJOS **Discovery** expander — enable/pause, countries, themes, industries, exclude keywords, save.
- `CLOUD_DISCOVERY_AUTOMATION.md` + `automation/ajos-discovery-setup.md` — full Cursor Automation runbook.
- `DISCOVERY_AGENT.md` + README updated — cloud primary, `/loop` legacy.
- **Your step:** create automation in Cursor dashboard using checklist; push config after UI edits.
