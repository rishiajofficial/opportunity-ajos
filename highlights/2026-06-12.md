# AJOS Daily Highlights — 2026-06-12

## Top 10

1. **Main nav tabs** — Learning, Proposals, Settings out of sidebar into sticky top tabs; sidebar = filters + queue only.
2. **Email finder follow-up run** — Cloud + local runs: Graphy/Futurice processed, 0 emails; Apollo 403 on `/people/match` and `/mixed_people/api_search` (plan/key tier); RemotePass still no contacts file; Kyan Health now in queue.
2. **Local fixes: draft height, Claude, contacts** — Message box 360px; `anthropic` installed; `app_secrets.py` reads `secrets.toml`; Kyan contacts added; Apollo `people/match` 403 on current plan.
2. **localhost:8501 back up** — `ImportError: UNCLEAR_RATING` was stale Streamlit process; restart fixed it (constant already in `learning.py`).
2. **Interested flow → 3 steps** — Next step → Draft → Send with ← Back / ← Queue; no more one long scroll page.
2. **Chat brevity + scroll fix** — max 2 short bullets, casual hi → 1 line; chat panel fixed height so UI doesn't stretch.
2. **Email Finder Agent (Apollo API)** — `email_finder_engine.py` + cloud runbook; enriches Liked companies via Apollo credits.
2. **LLM-powered company chat** — gpt-4o-mini with grounded context; rule-based fallback when no API key.
2. **Development Feedback Agent** — submit feedback in dashboard, approve in proposals, queue triggers dev agent.
2. **Repeated learning questions fixed** — latest-answer check, skip button, summary when core questions complete.
3. **`dev_feedback.json` + `dev_agent_queue.json`** — JSON queue for cloud/local Cursor automation.
4. **`DEV_AGENT.md` + `CLOUD_DEV_AUTOMATION.md`** — runbook for implementing approved feedback.
5. **Dev proposals in Learn tab** — Approve / Dismiss with queue confirmation message.

---

## Session log

**Decided**
- Dev feedback flows: submit → pending proposal → Approve queues agent → push JSON to GitHub for cloud pick-up.
- Answered questions use latest answer only; empty latest re-opens; Skip records a placeholder answer.

**Built & shipped**
- `apollo_client.py` — people/match + api_search fallback; `APOLLO_API_KEY` from env.
- `email_finder_engine.py` — queue from Like companies, run CLI, action draft sync, credit caps.
- `EMAIL_FINDER_AGENT.md`, `CLOUD_EMAIL_FINDER_AUTOMATION.md` — cron `0 10,22 * * *` UTC.
- `data/email_finder/config.json`, `runs.json` — enabled, max 10 credits/run.
- `contact_discovery.py` — email metadata fields + `save_company_contacts`.
- `app.py` — contact card shows email, status, source link.
- `llm_chat.py` — OpenAI gpt-4o-mini, context builder, Hinglish bullet parser, error handling.
- `company_chat.py` — LLM router with rule-based fallback; multi-turn history support.
- `app.py` — chat spinner, AI vs research mode caption, profile passed to chat.
- `llm_chat.py` / `app.py` — shorter Hinglish replies (≤12 words/bullet), scrollable chat box, history cap.
- `app.py` — focus view step wizard (overview / draft / send), back + return-to-queue navigation.
- `requirements.txt`, secrets example, README — `OPENAI_API_KEY` setup + cost note (~$0.25–2/mo).
- `learning.py` — `is_question_answered`, dev feedback CRUD, `dev_proposals` in `generate_proposals`.
- `app.py` — Dev feedback panel, dev proposals UI, improved learn tab (no repeat question form when complete).
- `data/learning/dev_feedback.json`, `dev_agent_queue.json` — empty stores.
- README learning section updated.

- `app_secrets.py` — shared env + Streamlit + `secrets.toml` reader for CLI and app.
- Draft step textarea height 150→360px; `pip install anthropic` for Claude chat.
- `data/contacts/companies/kyan-health.json` — Vlad Gheorghiu + co-founders; action syncs name but Apollo 403 blocks email fill.
- Streamlit restarted on :8501 after `content_engine` → `learning.UNCLEAR_RATING` import error on old process.

**Learned & open**
- Apollo key authenticates but returns 403 on enrichment/search — likely free tier or scoped key; needs Basic+ plan with master API key for `/people/match`.
- Queue: RemotePass (no contacts file), Graphy, Futurice, Kyan Health — 2 contacts each missing email where file exists.
- Cloud dev env set up: `pip install -r requirements.txt`, run `python3 -m streamlit run app.py`; login needs `.streamlit/secrets.toml` with `AJOS_PASSWORD`. Verified login + Interested feedback end-to-end. Notes added to AGENTS.md `## Cursor Cloud specific instructions`.
- Set `HUNTER_API_KEY` in Cursor Automation env for contact discovery + email finder; Apollo optional.
- First run: `python email_finder_engine.py run --max 1 --dry-run` then without dry-run (~3–6 credits).
- Set `ANTHROPIC_API_KEY` (or `OPENAI_API_KEY`) in Streamlit Cloud secrets for AI chat.
- Streamlit Cloud still needs commit/push of queue JSON for cloud automation to see approvals.
- Cron or manual Cursor automation setup is one-time per [`CLOUD_DEV_AUTOMATION.md`](CLOUD_DEV_AUTOMATION.md).
